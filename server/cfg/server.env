# QuoteView 快照服务端配置（compose env_file 加载）
TZ=Asia/Shanghai
QV_PORT=8388
QV_DATA=/data
QV_LOG=/log/server.log
# 每合约快照保留天数
QV_RETAIN_DAYS=7
# 相邻两次东财请求的间隔（秒）
QV_FETCH_GAP=1.5
# Web 管理台口令（HTTP Basic，用户名任意）。仓库不放真实值，只在 NAS 的这份里填。
# QV_ADMIN_PASSWORD=
