<Window x:Class="StockClient.App.Views.UpdateToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        FontFamily="Microsoft YaHei" FontSize="12"
        TextOptions.TextFormattingMode="Display">
  <Border Background="#F5161B26" BorderBrush="#33405C" BorderThickness="1"
          CornerRadius="8" Padding="16,12">
    <StackPanel Width="272">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="⬆" Foreground="#4C8DFF" FontSize="14" Margin="0,0,7,0"
                   VerticalAlignment="Center" />
        <TextBlock x:Name="TitleText" Foreground="#EDF1F7" FontSize="13"
                   FontWeight="SemiBold" VerticalAlignment="Center" />
      </StackPanel>
      <TextBlock x:Name="NotesText" Foreground="#8B93A3" FontSize="11" TextWrapping="Wrap"
                 MaxHeight="52" Margin="0,6,0,10" TextTrimming="CharacterEllipsis" />
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="UpgradeButton" Content="后台升级" Padding="12,3" Click="Upgrade_Click" />
        <Button Content="关闭" Padding="12,3" Margin="8,0,0,0" Click="Dismiss_Click" />
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
