# 更新日志

本项目的版本更新记录。发布流程见 [docs/RELEASE.md](docs/RELEASE.md)。

## v1.0.67

- **修"拖拽排序时灵时不灵"的反馈缺失**：根因是列排序视图下拖拽排序被设计为禁用（屏幕顺序≠实际
  顺序，移动无意义），但此前是**静默不动**，看起来像坏了——点过列标题排序后就"再也拖不动"。
  现在排序状态下尝试拖行会在**右下角弹出自动消失的状态提示**（不挡操作、点不到、3 秒淡出）：
  "当前为列排序视图，无法拖拽调整顺序；再点击排序列标题（至第三次）恢复原序后即可拖拽"。
- 该提示组件为通用能力，后续其它需要"拒绝手势但要说明原因"的场景可复用。

## v1.0.66

- **面板模板模型修正：面板永远在使用某个模板**，去掉多余的「（当前设置）」中间态。
  - 下拉选中即**切换使用**该模板（面板立刻变成它已保存的外观）；编辑仍写入草稿、点「保存」才落库并生效；
  - 面板右键的模板子菜单给**当前使用中的模板打勾**；
  - 旧配置升级时，现有面板外观自动收编为名为「默认」的模板并设为使用中，无感迁移；
  - 模板至少保留一个（删除使用中的模板自动切到剩余第一个）；「使用中的模板」随账户设置同步。

## v1.0.65

- **账户对话框重构**：
  - 登录态不再直接铺开修改密码表单——顶部三个动作按钮「修改密码 / 切换账号 / 退出登录」，
    点「修改密码」才展开旧/新密码输入；
  - **支持多账户切换**：客户端现在记住**每一个**勾过"记住密码"的账户（DPAPI 各自加密）。
    「切换账号」优先展示已记住的账户列表，**点名字一键切换**（免输密码）；「其他账户…」
    进入手动输入表单。未登录状态下若有记住的账户，打开对话框直接是账户快选列表；
  - 已记住的密码失效时（如被改密），该账户自动从快选列表移除并转入手动表单（用户名预填）。

## v1.0.64

- 分组整体涨跌幅**只计 A 股**（沪/深/北交所），港/美/韩股从篮子里排除——港股是另一个交易时段，
  美/韩股的涨跌幅更是"另一天"的数字，混进盘中篮子得到的是没有意义的值。分组里的非 A 股
  照常显示行情，只是不参与整体涨跌幅计算。

## v1.0.63

- 长连接 keep-alive 心跳 30 秒 → **5 秒**；服务端死连接判定相应收紧到 **20 秒**（连丢 4 个心跳）——
  "无声消失"（拔网线/断电）的感知上限从 ~95 秒降到 ~20 秒。正常关闭仍是即时感知。

## v1.0.62

- **客户端与服务端建立常驻长连接（WebSocket）**：登录后客户端主动建连并保持（内置 30 秒
  keep-alive ping），**连接本身就是"正在运行"的信号**——无论主动退出、进程被杀还是系统关机
  （FIN/RST），服务端**当下即感知**并更新会话页的活跃状态；断线自动重连（5s→60s 退避），
  登录/登出/令牌续期即时切换连接身份。这条连接同时是将来服务端主动推送的通道。
  - 实现：客户端用 .NET 内置 `ClientWebSocket`（无第三方依赖），服务端纯标准库实现 RFC6455；
    穿 EdgeOne CDN 与 nginx 反代已全链路实测（含断开即时感知）。
  - 物理边界如实说明：拔网线/断电类"无声消失"任何 TCP 都感知不到，靠 keep-alive 超时约 95 秒兜底。
- 会话页「活跃」判定升级为**长连接在线**（即时）；无长连接的旧版客户端仍按心跳 3 分钟判定。

## v1.0.61

- **区分「在线」与「活跃」两个指标**：在线 = 登录状态（定义不变）；新增**活跃** = 客户端此刻
  正在运行——客户端登录后每 **60 秒心跳**一次，会话页每条会话标注 活跃/挂起（3 分钟无心跳判挂起），
  账户状态行显示「在线 N · 活跃 M」双计数。配合页面 10 秒自动刷新，"现在几台连着"准实时可见。
- 心跳顺带把**服务端令牌丢失的自愈时间从 ≤5 分钟缩到 ≤1 分钟**（心跳撞 401 即静默重登）。

## v1.0.60

- **会话「在线」语义按定稿修正**：在线 = 客户端处于登录状态（令牌有效），登出/被踢/过期才算下线，
  不再按"10 分钟内有活动"判定；「在线时长」改为「登录时长」（自登录时刻起算），最近活动列保留作参考。
- 配套收尾：客户端**登录续期时主动登出被替换的旧令牌**——否则每次重启/续期都会在会话列表里
  多挂一个 30 天不散的"幽灵在线"；存量死令牌已服务端一次性清理。

## v1.0.59

- 模板支持**重命名**（模板栏新增按钮，仅选中模板时可用）；重命名不影响未保存的编辑草稿，
  也不会把半成品改动悄悄应用出去。

## v1.0.58

- **模板交互按定稿重做**：「另存为」改成「**新建**」——新模板以**默认配置**起步（字段白色、
  涨红跌绿），选中后进入**缓冲编辑模式**：所有改动只写入模板草稿、不影响面板，点「**保存**」
  才落库并应用到面板。下拉切回「当前设置」则恢复原来的即改即生效。保存/删除按钮仅在
  选中模板时可用。
- Web 管理台自动刷新 30 秒 → **10 秒**（仍只刷新当前激活页签）。

## v1.0.57

- **历史分时支持对比**：同一合约选两个日期叠加显示——对比线（蓝色）按**各自昨收归一化**到同一
  百分比坐标（不同日期绝对价位不同，直接叠没有意义），图上带日期图例，坐标范围自动容纳两条线。
- **快照带当日指标**：服务端收盘归档时用一次腾讯批量请求为全部快照补当日**涨跌幅/成交额/成交量/
  外盘/内盘**（今日 186 个存量快照已全部补齐）；历史分时页图上方显示指标条，对比时两行（白/蓝对应两日）。
  旧快照没有指标的显示"当日指标暂无"，客户端发现缓存缺指标会向服务端重取一次。
- **简洁面板支持模板**：面板设置顶部新增模板栏——「另存为…」把当前整套设置（字段/颜色/行数/行距/
  透明度/图表）存成命名模板，下拉即切换，可删除；**面板右键新增「模板」子菜单**一键快速切换。
  模板不含面板位置（跨机分辨率不同）；模板随账户设置同步，多端共享。

## v1.0.56

- **登出计入日志**：客户端「退出登录」现在会通知服务端（令牌立即失效并记 info 级「登出」；
  此前只在本地丢弃令牌，服务端要等 30 天自然过期）；管理台登出记「管理台登出」；
  管理员强制登出记 warn 级「被管理员登出（操作者）」，三条路径都进日志管理页。
- 管理台登录统计的**独立 IP 展开为去重后的具体 IP 列表**（数量 + 逐个列出）。

## v1.0.55

- **同步模型按最终定稿简化**（废弃 v1.0.53/54 的时间戳仲裁与双向对账）：
  - 客户端**仅在登录时从服务端拉取一次**个性化配置与分组，无条件采用服务端副本（新账户无数据则用默认）；
  - 之后**只有客户端的修改行为会推送**（2 秒防抖 + 失败 30 秒重试 + 退出冲刷 + 分组每 5 分钟例行上报）；
  - **服务端自始至终不反向同步**，运行中不再有任何"从服务端拉下来"的动作；
  - 多端并发修改：**按推送到达顺序覆盖**，后推者胜；另一台在下次登录/重启时拉到最新副本。
  - 明确的取舍：未登录/离线时的本地改动，会在下次登录拉取时被服务端副本覆盖。

## v1.0.54

- **修 v1.0.53 之后两机仍不收敛**（列设置、分组轮换标记对不上）。对账逻辑还剩两个洞：
  - **设置对账是单向的**：只有"服务端新→拉"，没有"本地新→推"。未登录/离线时改的设置被本地
    时间戳保住后，既拒绝服务端旧副本、又永远不上传——两台机器各自守着自己的版本僵住。
    现在设置与分组一样**双向**：本地新就推。
  - **时间戳相等但内容不同会死锁**（如两台同期升级、戳都为 0）：谁也不拉谁也不推。
    现在此情形**重新打戳并上推**，一台成为权威、另一台在下轮对账拉齐。
- 两台机器都升级后最迟 10 分钟（两轮对账）内收敛；之后任一台的改动 5 分钟内同步到另一台。

## v1.0.53

- **修多机同账户不收敛**（corp-win 和 home-win 各显各的）。根因两个：分组只在"换属主"时才下载，
  两台机器各自上推、从不下拉，服务端副本被轮流覆盖；设置只在启动时拉一次，两台同时开着时互相看不见
  对方的改动。现在改为**双向对账**：
  - 分组与设置都带**修改时间戳**，客户端**每 5 分钟与服务端比对**：服务端新 → 拉下来**运行中即时生效**
    （分组列表/行情表/简洁面板/列布局/备注/分组栏宽度全部在线刷新，不用重启）；本地新 → 推上去；
  - 服务端**拒绝旧盖新**（过期推送回 409），杜绝两台机器 ping-pong 覆盖；
  - 改动仍即时推送（2 秒防抖 + 失败 30 秒重试 + 退出冲刷），对账兜双机并发的场景。
- 分组同步补上**「加入简洁面板」标记**（此前恢复分组时会丢，全部变回勾选）。
- 简洁面板**位置不跨机同步**（屏幕分辨率不同，坐标同步过去可能直接飞出屏幕外），其余面板设置照常同步。

## v1.0.52

- **把"实时同步"做到位**：设置推送失败后**每 30 秒自动重试直到成功**（原来要等下一次改动才会再试）；
  **退出前冲刷**未落地的改动（防抖 2 秒内关 app 的场景，限时 3 秒不阻塞退出）。
  时间戳仲裁退化为纯保险，只兜"网络物理不可达"和"未登录修改"两种无法实时同步的情况。

## v1.0.51

- **客户端支持修改密码**：账户对话框（右上角用户名）登录状态下新增修改密码，**需校验旧密码**；
  成功后其他设备的登录立即失效、本机会话保留，「记住密码」自动跟随新密码。Web 管理台的
  管理员重置密码保持免旧密码（并记录操作者）。
- **修个性化设置被回滚**：列顺序本来就在同步载荷里（`Order` 即 DisplayIndex），真正的坑是
  启动拉取无条件"服务端胜"——**未登录/离线时调过的布局会被下次启动的拉取盖掉**。现在设置
  载荷带修改时间戳，**新者胜**：本地较新则跳过合并，离线改动不再丢。
- **管理台按成熟管理系统重构为三页签**：
  - **账户管理**：列表（角色/状态/数据量/设置同步/在线数）+ 新增/删除/禁用/登出/改密码/改角色；
  - **会话管理**：各账户状态 + **仅在线会话**（IP/客户端版本/登录时间/最近活动/在线时长），
    不再展示离线会话，空 IP 与 127.0.0.1 的历史脏条目一律过滤；
  - **日志管理**：登录日志（按用户/IP/日期过滤）+ 按用户汇总统计（次数/独立 IP/最近登录）+
    **密码修改日志**（含操作者：本人 or 管理员）。
- 服务端顺带：登录时清理 30 天无活动的陈旧令牌；密码修改计入日志（自助与管理员两条路径）。

## v1.0.50

- **未登录时仅保留「合约查询」**：实时行情/历史分时/资讯三个页签显示登录提示遮罩，行情轮询暂停；
  登录后即刻恢复。简洁面板依赖行情轮询，未登录时同样静默。
- **K 线改为服务端优先**：服务端新增 `/kline` 代理（参数白名单校验 + 短缓存，一次上游请求服务
  所有客户端；空响应不缓存防投毒）；客户端取数顺序变为 本地当日缓存 → 服务端 → 东财直连 → 腾讯直连
  （服务端不可达时图表照常）。历史分时此前已是服务端拉取+本地缓存。当天分时与 1 秒实时行情
  仍由客户端直连（按约定）。
- **切换用户的缓存策略**（评估结论落地）：K线/分时/历史快照等行情缓存与账户无关，**全部复用**；
  账户数据按账户隔离——设置由服务端合并覆盖；**分组带属主标记**：无主的本机旧数据被首个登录者
  收编，属主不同则从服务端恢复该账户的分组（服务端本就存有每个账户的分组）；
  分组上行同步加属主校验，杜绝 A 的分组被推进 B 的账户。
- **账户角色体系**：普通用户 / 普通管理员 / 系统管理员（admin 已设为系统管理员，全局唯一）。
  Web 管理台改用**账户密码登录**（与客户端一致，仅管理员角色可进）：普通管理员只能操作普通用户、
  不能改角色；系统管理员可操作所有人并修改角色（user↔admin）；系统管理员账户不可删除/禁用/降级。
  管理台按登录者角色显示可用操作，服务端独立校验不信任前端。

## v1.0.49

- **个性化设置跟账号走**：简洁面板字段/颜色、行情表列显隐与布局、合约备注、整体涨跌幅口径、
  分组栏宽度，登录后**启动时从服务端拉取合并**（拉取先于界面加载，各视图直接读到合并结果），
  之后**每次修改防抖 2 秒实时上传**（内容没变不发请求，颜色拖拽这类高频保存不会刷爆服务端）。
  换机器登录同一账号即可还原全部个性化配置。
- **服务端新增 Web 管理台**（`/quoteview/api/admin`，HTTP Basic，口令在服务端 cfg 配置）：
  - 账户列表：状态、分组/合约数、设置同步情况、**在线设备数与每个会话的 IP/客户端版本/登录时间/
    在线时长**（在线判定：10 分钟内有活动）、最近 50 条登录日志；30 秒自动刷新
  - 操作：**新增/删除/禁用（禁用即踢下线）/登出全部会话/改密码**（改密码可选是否同时登出该用户）
- 客户端每个请求带版本号头；服务端记录每次登录的 IP/版本（经 EdgeOne 的 X-Forwarded-For 取真实公网 IP）
- 修服务端一个并发丢失更新：会话活跃度回写与设置写入竞争时可能覆盖后者（锁内重读后再写）

## v1.0.48

- **服务端交互升级为账户体系**（替代 v1.0.47 的匿名客户端 id）：注册/登录（PBKDF2 哈希存储、
  Bearer 令牌、单账户多设备），分组同步与历史分时查询全部账户级鉴权。同一账户可多端登录，
  分组以账户为单位维护。
- 客户端右上角新增**「登录」按钮**：登录/注册对话框，支持**记住密码**与**自动登录**——凭据用
  Windows DPAPI（当前用户）加密存本机；令牌失效时用记住的密码**静默续期一次**，不弹窗。
- 未登录时一切照常：行情/K线/简洁面板不受影响，历史分时退化为仅本地快照并提示登录。
- 既有用户数据已在服务端迁移到 admin 账户（16 分组 204 合约无损）。

## v1.0.47

- **历史分时改为客户端-服务端架构**（v1.0.46 的客户端本地拉取方案废弃）：
  - NAS 上新增 QuoteView 服务端容器（`server/server.py`，纯标准库，nginx 反代 `/quoteview/api/`）；
  - 客户端启动时向服务端**协商唯一 id** 并持久化（`server-id.txt`），之后**每 5 分钟同步分组+合约**；
  - 服务端汇总**所有客户端合约的并集**（仅沪深），收盘后统一分批拉取当日分时并归档（串行 1.5s/只、
    节假日探测、保留 7 天）——多台客户端只产生一份抓取流量；
  - 「历史分时」页从服务端取数：日期列表为**服务端∪本地**，图数据本地缓存优先、缺了才向服务端拉一次
    并写回本地缓存；服务端不可达时页面退化为只显示本地已有快照，不弹错。
  - 客户端不再自行做收盘扫描（删除 TrendSweeper）；所有同步/查询失败均静默，不影响主功能。

## v1.0.46

- 新增**「历史分时」页签**：按 分组 → 合约 → 日期 查看沪深合约往日的分时图（复用现有分时图控件，
  含均价线/成交量/十字光标）。页面**纯读磁盘、零请求**。
- **收盘后自动快照**：A股收盘约 20 分钟后，后台把全部分组的沪深合约当日分时批量拉一遍并落盘
  （`trends\{代码}\{日期}.json`，沿用 7 天保留期）。流控：严格串行、每只间隔 1.5 秒（约 40 只/分钟）、
  每轮至多 150 只，10 分钟一轮直到补齐；用户看过的合约已由现有逻辑落盘、直接跳过不发请求。
  失败的下一轮重试；重启 app 自动续拉（文件存在即完成，无需额外状态）。
- **节假日自动识别**：分时点位自带完整日期，工作日拉到的若是旧交易日的数据即判定当天休市，
  一只探测就停止整批，不落错误日期的文件。周末不启动。
- 其他市场（港/美/韩/北交所）按约定不参与快照；照常可在图表窗口看当天分时。

## v1.0.45

- 修 v1.0.44 **分组名被涨跌幅挤到看不清**：整体涨跌幅从名称右侧移到**分组名下方第二行**（小字号，
  红涨绿跌），名称拿回整行宽度；还没算出涨跌幅时第二行收起，行高不变。
- 分组栏与行情表之间加**拖动分隔条**：分组栏宽度可在 170~480px 间拖调，拖完自动保存、重启还原。

## v1.0.44

- 每个分组显示**整体涨跌幅**（分组列表名称右侧，红涨绿跌）——像上证指数那样把一组合约当一篮子算。
  - 默认**流通市值加权**，抄中证指数的口径并化简：权重取**昨收流通市值**（今日市值÷(1+涨幅)反推，
    否则涨的票会放大自己的权重）；停牌合约照指数惯例按 0% 计入；缺市值的合约按组内平均权重兜底。
  - 分组列表**右键可切「等权平均」**（含义是"这组票的平均表现"），全局设置、即时生效、落盘保存。
- 为此**所有分组的合约并入 1s 轮询**（去重），不再只轮当前分组。实测腾讯批量接口无数量风控、
  唯一上限是 URL 8KB（≈900 个代码；850 实测 200/429ms、1000 实测 414），客户端按 800/批自动分块。
  非当前分组的行情只进聚合计算，表格/东财第二路/基准价仍只跟当前分组，UI 压力不变。

## v1.0.43

- 简洁面板**双击 = 亮度直降到 0**（完全隐形），等于连按 `Win+Alt+↓` 到底的一键版。
  隐形后鼠标会穿透面板点不回来，**恢复用 `Win+Alt+↑`**；隐形状态下滚轮切合约照常有效。
  单击拖动、右键菜单不受影响。

## v1.0.42

- 修**合约拖拽排序失效**：v1.0.29 为批量操作把列表选择模式改成了多选（Extended），而 WPF 里
  "按住拖过行"本身就是框选手势，和拖拽排序抢同一个动作——每次拖动都变成了扩选。现在明确分工：
  **直接拖行 = 排序移动；按住 Ctrl/Shift 拖 = 框选多选**（点选/Ctrl点选/Shift点选不变）。
  排序视图（点过列标题）下拖行依旧不排序（屏幕顺序和实际顺序对不上），但不再误触框选。
- 简洁面板**字段设置补齐到与主表一致**（46 项）：新增 昨日/3/5/10/20/60日/年初至今涨幅、
  涨速、主力净流入/占比、超大/大/中/小单、内外盘、涨停/跌停价、52周高低、股息率、
  行业/地区/概念、备注。涨幅与资金类字段支持红涨绿跌双色；概念/备注超长截断，防止面板被撑爆。
- 资金类字段的**按需轮询打通简洁面板**：原来只有主表格的资金列可见才开第二路东财轮询，
  面板勾了主力净流入但表格列隐藏时会一直空白。现在两处任一勾选即开。

## v1.0.41

- 简洁面板右键新增**「分组」子菜单**：直接点名切到任意分组，不用再靠 `Win+Alt+PageUp/PageDown`
  一个个轮过去。每项带合约数量，当前分组打勾。
- 菜单**每次打开时重建**：分组会在主窗口里增删改名，构造时建一次的菜单只会给出过期的列表。
- 没勾「加入简洁面板」的分组**照样列出**并标注「不参与轮换」——手动选中和轮换是两回事，
  能在主窗口里看见却在面板里选不到才是怪事；标注则是为了下次 PageDown 跳走时不像 bug。

## v1.0.40

- 修取色器**一点开就立刻消失**：弹窗开在 `MouseDown` 上，而 `StaysOpen=false` 的 Popup 打开后
  立即接管鼠标捕获，紧跟着的那次抬起落在弹窗之外，被 WPF 判成"点了外面"——同一次点击开完又关。
  现在改为在**抬起**时开，并且关闭逻辑自己管（点设置窗口任意处 / 滚轮 / 窗口失焦 / 完成 / Esc），
  不再依赖 Popup 的鼠标捕获——那套捕获同时也在跟色板拖拽抢鼠标。
- 同一时刻只会有一个取色弹窗；十六进制框里回车或 Esc 直接关。

## v1.0.39

- 修上一版取色器**色块和十六进制看不见**：App.xaml 合了 WPF-UI 的 ControlsDictionary，原生
  `Button`/`TextBox` 都被换成内边距很大的模板（项目里的 `ui:TextBox` 因此都写 `Height="36"`），
  而我把取色控件钉死在 24/22 像素高，内容被自身模板的 padding 挤出了可视区。
  现在色块按钮改为自绘（Border，深色描边 + 悬停高亮），十六进制输入框不再写死高宽。
- 顺带两处布局：字段列表的 `ScrollViewer` 水平滚动由默认的 `Hidden` 改为 `Disabled`——WPF 里
  `Hidden` 仍按**无限宽**测量内容，行不会收缩到视口，右侧颜色框迟早被裁；设置窗口宽度 380→430，
  「分组名（面板左侧）」这种长标签配两个取色框也不再局促。
- 新增 `tools/Preview`：无桌面会话的**离屏 UI 渲染器**（RenderTargetBitmap 直出 PNG）。
  SSH 落在 session 0，起不了可见窗口，这个工具让改完的界面在发版前能真的看一眼。

## v1.0.38

- 简洁面板设置里的**字段颜色改成完整取色器**：饱和度/明度色板 + 色相条 + 十六进制输入框，
  1600 万色都能选，不再是七个固定色的下拉。原来那七个色留作快捷预设（第一排），点一下就是老配色。
- 拖动色板时**面板实时跟着变色**，不用先关掉弹窗再看效果；十六进制框可直接粘贴 `#RRGGBB`。

## v1.0.37

- 简洁面板的**分组名现在可以在设置里调颜色和显隐**了，和其它字段一样——上一版把颜色硬编码成灰色，
  唯独它调不了。设置窗口里叫「分组名（面板左侧）」。
- 「至少保留一个字段」的保护只统计**行内字段**：分组名画在行的旁边而不是行里，只勾它的话行仍然是空的，
  而单独关掉它没有任何问题。

## v1.0.36

- 简洁面板**左侧显示当前分组名**。面板用 `Win+Alt+PageUp/Down` 循环分组，光看合约认不出落到了哪一组。
  名称与行情行**垂直居中并排**，不是加一行标题——面板最缺的就是高度；过长省略号截断。

## v1.0.35

- 修资讯页**刷新拿不到新版简报**：客户端是缓存优先，同一天的简报一旦缓存就再也不去拉了——
  而当天的简报本来就会被重新生成（07:30 一班、15:30 一班，外加手动重跑），于是刷新多少次
  都是旧的那份。现在**最新那天每次都重新拉**，更早的日期不会再变，继续走缓存不发请求。
- 拉取失败时回退到缓存（旧简报好过空白页）。

## v1.0.34

- **资讯改为走 HTTP 拉取**，和自更新同一条通道：`nas.pcguan.cn/quoteview/brief/`。
  上一版是把简报 scp 到指定几台机器的本地目录——那只覆盖得到管道能 SSH 登录的机器，
  别的客户端根本拿不到数据，不算分发。现在任何装了 exe 的机器都能读，无需任何配置。
  - `index.json` 列出可用交易日，客户端据此发现日期，不靠猜文件名
  - 下载的简报缓存在 `%APPDATA%\StockClient\brief\`（保留 30 天），断网时仍可翻看已读过的
  - 先渲染缓存再拉新的，页面不会因为等请求而空着

## v1.0.33

- 资讯页的**日期选择框**：改为显示 `2026-08-05` 而不是 `20260805`，并加宽——原来的窄框会把日期切掉。
- **数据源异常提示**的成因修掉了：证券时报快讯列表页是客户端渲染的，HTML 里根本没有文章链接，
  站点也没有对应列表接口（唯一的 ajax 端点服务信披且返回空），所以这个源被移除。
  一个永远取不到内容的源比没有更糟——它每天报一次异常，久了就没人看异常那行了。
  （财联社同样移除：对非浏览器客户端返回 418。）现在三个源：东财快讯 / 新浪 7×24 / 巨潮公告。
- 管道加了**发布前校验闸门**（`verify.py`）：JSON 是否合法、每条线索的来源文件是否真实存在、
  有无预测/建议类禁用词、指数数字是否与 `data/` 逐项一致。**不通过就不分发**，客户端宁可看昨天的。

## v1.0.32

- 新增**资讯**页签：展示每日信息聚合简报——交易详情（指数/广度/板块首尾/关注池）、利多线索、
  利空线索、**未证实信息（隔离）**、**反方视角**，以及数据源状态。可切换日期。
- 每条线索都显示**来源文件名**，可以回到原文核对；抓取失败的数据源在顶部显眼提示，
  相关内容按 N/A 处理，不用别的来源顶替。
- 客户端**只做展示**：读本地 JSON、不联网、不做任何计算。简报由仓库里的 `brief/` 管道生成
  （确定性抓取行情与白名单媒体原文 → 按硬约束填模板），产物推到 `%APPDATA%\StockClient\brief\`。
- 界面上没有预测、目标价、买卖建议的展示位——数据结构里就没有这些字段。

## v1.0.31

- 合约支持**备注**：行右键「添加备注…／编辑备注…」，新增「备注」列（默认隐藏，存过备注会自动显示出来）。
- 备注**按合约代码存**，不挂在分组上——所以同一只票出现在几个分组里都是同一条备注，改一处处处生效。
  存在 `groups.json` 里，纯本地，不外传。
- 清空备注会删掉这条记录而不是存个空串，配置不会攒下一堆死键；旧配置没有 `notes` 键也能正常读写。

## v1.0.30

- 新增**区间涨幅**列（默认隐藏，列设置里勾选）：**昨日 / 3日 / 5日 / 10日 / 20日 / 60日 / 年初至今**。
- 实现上没有每秒去要这些数——真正静态的只是**基准价**（N 个交易日前的那根收盘），
  所以每个交易日**批量拉一次**（整组一个请求）反推出基准存盘，之后涨幅在本地用实时价算，
  **跟着行情每秒更新**，不会停在早上那一刻。实测本地算出的值与东财当时直接给的**逐项零误差**。
  - 按**各自市场的交易日**刷新，不是一个全局"今天"——否则美股每天晚上都会把沪深重拉一遍。
  - 断网/限流时用上次存盘的基准继续算，只是基准旧一天。
- **昨日涨幅**东财没有对应字段，用「今天的昨收 ÷ 昨天记录的昨收」推出来，同样零额外请求。
  代价：**第一天装上是空的**，次日起才有；长假后的第一个交易日也会空一天（宁可空着，也不把一周的
  涨幅标成昨天的）。
- **韩股不提供**这些字段：实测东财对韩股六个周期返回同一个数（都是 -3.35），属于假数据，直接拒掉。

## v1.0.29

- **合约支持批量操作**：实时行情列表可多选（Ctrl/Shift 点选、框选），右键对**整批**执行
  移动到分组 / 添加到分组 / 从本分组移除。菜单顶部写明作用对象（「选中的 5 个合约」），
  目标分组标注「已有」或「缺 N 个」；整批只保存一次、只刷新一次列表。
  - 右键点在已选中的行上保持整批选中；点在选区外则只作用于那一行——否则右键会把多选悄悄缩成单选。
- **点击合约不再挤动上方列表**：下方详情区原来随选中出现/消失，每次点击都会改变列表高度、
  行在光标底下跳。现在详情区**固定高度、常驻**（未选中时显示提示），字段多了内部滚动。
- **五档盘口从详情区移到图表窗口**：双击合约打开的窗口里，切到「分时」时**左边分时图、右边五档**，
  跟着 5 秒轮询实时刷新；切回 K 线时整列收起，图表拿回全部宽度。
- **重做五档的排版**：原来档位和价格挤在一起、中间一大片空白、挂单量孤零零贴在右边，
  而且档位字号比价格小导致每行看着像两行。现在是三列对齐（档位 | 价格右对齐 | 挂单量右对齐），
  统一字号和基线，量能横条改为从右侧生长，作为背景不再挤压数字。

## v1.0.28

- **被别的程序占用的快捷键，现在会被接管**（启动时抢过来，退出时原样还回去）。
  Windows 的 `RegisterHotKey` 是先到先得，既没有抢占接口，也没有"临时挂起别人的注册"，
  所以对注册失败的组合改用**低级键盘钩子**——它在系统派发热键之前就看到按键，吞掉即压过原占用者；
  退出时卸载钩子，占用者的热键立刻恢复。
  - 只对**真正被占**的组合装钩子；全部注册成功时一个钩子都不装。
  - 回调只做一件事：比对虚拟键码 + 两个修饰键状态，然后投递。不读取、不保存、不转发任何按键。
- 面板右键菜单原来只要有一个热键失败就说「已停用」，实际上注册成功的照常能用——现在改成
  只有接管也失败时才提示，并写明是几个。

## v1.0.27

- 分时图**收盘后落盘缓存**：某个市场收盘之后当天的分时就定型了，第一次抓到就写进磁盘
  （`%APPDATA%\StockClient\trends\{代码}\{日期}.json`，保留 7 天），当天再打开、重启 app 都**零请求**。
- 盘中**不落盘**：序列还在长，而两个源都只能"一次返回全天"、要不到增量，存半天的没有意义。
  所以磁盘上存在即定稿，读出来可以直接用。
- 定稿的数据在内存里也**不再按 15 秒过期**（当天不会再变了）。

## v1.0.26

- **滚轮改用低级鼠标钩子**，彻底绕开 Windows 的滚轮路由。日志实测：正常时每一格都有 `WM_MOUSEWHEEL`，
  失效时**连着 18 秒一条都没有**——是系统不再把滚轮发给这个窗口了。系统只在鼠标**移动**时决定
  "滚轮归谁"，而面板是个从不激活的分层置顶窗口，那份判定一旦坏掉，光标不动就再也刷新不了
  （所以挪一下鼠标就好）。消息根本没发出来，在 WPF 层还是窗口消息层接都没用。
  现在由面板自己判断：光标在面板矩形内就切合约并吞掉事件，在别处原样放行。
- 去掉滚轮路径上的同步日志（上一版为诊断加的，本身就会让滚动变钝）；改为计数，由 5 秒心跳汇报
  `wheel=命中/放行 hook=on|off`。

## v1.0.25

- 滚轮改为在**窗口消息层**（`WM_MOUSEWHEEL`）处理，绕开 WPF 的路由。WPF 的滚轮事件必须从"光标下的
  元素"开始冒泡，而面板每次刷新都在重建行、那个元素一直在消失；窗口消息不受这个影响。
- 上一版的 `Mouse.Synchronize` 调早了——新行刚加进去还没完成布局，命中测试自然找不到东西。
  改成布局完成后再做。
- 滚轮两条路径（窗口消息 / WPF 路由）各记一行日志，失效时能直接从 `panel.log` 看出断在哪一层。

## v1.0.24

- 真正修好**滚轮停在原地滚几下就不动、挪一下鼠标又能滚**。跟灵敏度和性能都无关：
  面板每次刷新都会把所有行**丢弃重建**，如果光标正停在某个字上，WPF 的 mouse-over 元素就被移出了
  可视树；而滚轮是**路由事件**，要从光标下的元素开始冒泡——没有起点，事件就到不了窗口。
  光标没动，系统也不会重新做命中测试，所以只能靠挪鼠标恢复。
  现在每次重建后**强制重做一次命中测试**（`Mouse.Synchronize`），新元素立刻接管。

## v1.0.23

- 继续修**滚轮越滚越钝**（光标始终在窗口内的那种）。这次是 UI 线程被自己的活儿压住了，两处：
  - **每切一个合约就立刻抓一次分时**（切换会把 20 秒节流清零），滚过 20 个合约就是 20 次请求，
    每次还带一条**同步写文件**的诊断日志（panel.log 已有几 MB）。改成**滚动停下来 400ms 才抓**，
    途中一个请求都不发；继续滚就继续顺延。
  - **渲染按滚轮次数排队**：每个滚轮格子都往 Dispatcher 里投一次重绘，和每秒行情刷新挤在一起，
    绘制越落越后。改成**只排一次、画最新状态**，中间帧直接丢弃。

## v1.0.22

- 修复**滚一会儿滚轮就没反应**（光标压根没动）。面板宽度是自适应内容的，每个合约的名字长度不同，
  滚到名字短的合约时窗口右边界会**往回缩**，光标原地不动却落到了窗口外面，滚轮就交给下面的窗口了。
  现在面板宽度在同一分组内**只增不减**，切分组时重新开始。

## v1.0.21

- 简洁面板**去掉左/右键切分组**（与左键拖动、右键菜单重复，且两者都有快捷键）：左键恢复拖动面板、
  右键恢复打开菜单、中键不再有特殊作用。
- **只保留滚轮切合约**，并修掉不灵敏：
  - 快速拨动时 Windows 会把多格合并成**一个** `Delta`（240/360…），原来不管多大都只走一格，
    滚得再快也只跳一个合约。现在按 `Delta/120` 算格数，一次到位（仍只重绘一次）。
  - 面板只有 20~37px 高，滚动时光标很容易滑出窗口、滚轮就落到下面的窗口去了。
    把面板的**可命中区域加高**（Padding 2→8），背景几乎全透明所以看不出变化。

## v1.0.20

- 简洁面板支持**鼠标操作**，不用记快捷键：
  - **滚轮上/下** —— 上一个 / 下一个合约
  - **左键单击 / 右键单击** —— 上一个 / 下一个分组
  - **左键拖动** —— 仍然是移动面板（按移动距离区分单击与拖动，超过 4px 才算拖）
  - **中键** —— 打开菜单（原来在右键，让位给切分组；还原主窗口另有托盘图标双击）
- 原本单击被拖动整个吞掉、没有任何反馈，现在单击有了明确作用。快捷键全部照旧。

## v1.0.19

- 面板分时抓取加**诊断日志**（`panel.log`）：合约未找到 / 两源都没数据 / 抓取异常 / 成功拿到多少点，
  各记一行（约 20 秒一次）。这条链路原来所有失败都是静默的——图就那么空着，从外面看和"画图坏了"
  没法区分，这次就是这么被误判的。

## v1.0.18

- 修复**简洁面板分时缩略图画不出来**（K 线窗的分时图同样受影响）。根因是**东财 `trends2` 路径被限流**
  （实测该出口连接直接被 reset，同主机的 kline 路径却还通），而分时只有单一数据源、失败后 UI 一片空白，
  看起来就像图坏了。
  - 分时改为**双源**：东财优先，失败/空结果自动切**腾讯 `minute/query`** 兜底（A股/港股完整分钟序列，
    美股/韩股只有一点）。东财这条路径也补上了 3 次重试（和 K 线一致，对付瞬断）。
  - K 线窗的分时图改为走同一个仓库，因此同样有双源兜底 + 15 秒缓存。
  - 两源都没数据时，缩略图**画一行「分时暂无数据」**而不是留白——空白无法与"图坏了"区分。
- 缩略图可读性：线加粗（1.1→1.6px）并**填充到昨收基线**。面板不透明度可以调到 20%，一条细线在那个
  透明度下几乎看不见；而且横轴按**整个交易日**归一化（为了不同合约可比），早盘时线本来就只占很短一段。
- 腾讯兜底不提供均价（北交所/美股/韩股），分时图据此**跳过均价线**、也不把 0 计入价格轴范围
  （否则轴被拉到 0～2 倍昨收，价格线被压成一条平线），十字光标里均价显示「-」。

## v1.0.17

- 新增**五档盘口图**：卖五→卖一在上、买一→买五在下，每档一条按挂单量比例的横条，价格按昨收红涨绿跌。
  数据就在现有的 1 秒行情响应里（腾讯 `[9]–[28]`），**不增加任何请求**，所以刷新频率与实时行情完全一致。
  - 实时行情下方详情区常驻显示（跟随选中行）。
  - 简洁面板也能显示：**Win+Alt+Delete 现在是三态循环** —— 关闭 → 分时缩略图 → 五档盘口 → 关闭。
    面板右键「面板图表」和设置窗里也能直接选。
- 各市场实际能给的盘口不同（实测）：沪深北 五档价+量齐全；美股只有买一卖一；港股只有一档且量恒 0、
  价格其实就是现价回填（没有真实盘口）；韩股整段为空。后两种都直接写明「该市场不提供盘口」，
  不画成一堆 0，也不把现价回填画成两行"买卖价"。

## v1.0.16

- 实时行情的合约行支持**右键菜单**：**移动到分组** / **添加到分组**（列出其它所有分组，
  目标已有该合约时标「已有」——添加置灰、移动仍可用，因为它还要把本组这份去掉），
  外加「打开 K 线图」「从本分组移除」。
- 菜单挂在数据行上，表头右键仍是「列设置…」；右键会先选中该行，下方详情面板跟着走。

## v1.0.15

- K 线图**盘中不再画当天那根**——盘中的当日 K 线是没走完的（收盘价=当时价，最高/最低/成交量都只到那一刻），
  现在图直接止于上一交易日；**收盘后读一次当日收盘价并缓存**，当天不再请求。
- 因此 v1.0.14 的"顶更末根"机制取消，逻辑更简单：一个合约每天最多两次全量拉取（盘中开过一次、收盘后一次）。
- K 线窗开着跨过收盘时，30 秒内自动把当日那根接上，不用重开窗口。

## v1.0.14

- 修复 **K 线图当天数据不刷新**：K 线缓存原来只按交易日存一次，盘中打开图表就把**当天那根未走完的**
  K 线（收盘价=当时价、最高最低与成交量都不全）钉住一整天，收盘后再看还是盘中的半截数据。
  现在缓存会记录抓取时刻：收盘前抓的数据只**顶更最后两根**（几百字节，不重拉全量历史），
  收盘后抓的才当天有效。
- K 线窗口开着时每 30 秒**静默刷新**当天 K 线，且**保留当前缩放与平移**（原来只在打开/切周期时加载一次）。

## v1.0.13

- 实时行情新增 **A股涨速 / 主力资金** 列（涨速、主力净流入、主力占比、超大/大/中/小单，默认隐藏、红涨绿跌）：
  在腾讯 1 秒行情之外挂一条**东财 5 秒批量轮询**，**仅在勾选相关列时才启动**、只请求 A 股、失败静默退避，
  不影响主行情。
- 简洁面板可选字段补齐：新增 成交量/成交额/总市值/流通市值/换手率/量比/振幅/均价/市盈TTM/市净率
  （大数进位显示）；设置窗字段区加滚动，字段多了也不超屏。

## v1.0.12

- 列设置拖拽改为**鼠标捕获式**：浮影全速率逐像素跟手（原 OLE 拖放事件节流导致移动时卡片不动）。
- 齿轮按钮改为**悬浮在表头行右端、始终右对齐**——不再受列总宽影响，调整窗口宽度也贴右。

## v1.0.11

- 列设置入口改为**齿轮图标**，位置移到表格**列名那一行的末尾**（原右上角按钮移除，表头右键仍可打开）。
- 列设置拖拽排序体验重做：拖动时**卡片浮影跟随鼠标**，原位置半透明占位，其余卡片实时让位——
  不再是毫无反馈、松手瞬移。

## v1.0.10

- 隐私：更新提示条不再显示更新来源（原「国内(NAS)」/「GitHub」字样），只显示版本号。

## v1.0.9

- 实时行情新增**列设置窗口**（表头右上角图标按钮 / 表头右键「列设置…」）：全部列平铺展示，
  点击切换显示，**按住拖动直接调整列顺序**；带 全选 / 全清 / 默认 快捷按钮
  （默认 = 代码、名称、最新价、涨跌幅、涨跌、行业）。替代原先会超出屏幕的纵向右键菜单。
- 更新检查节奏：**启动 1.5 秒后静默首查，此后每 30 秒轮询**；每次按 NAS 优先 → GitHub 回退，
  单源 10 秒超时、坏源快速跳过（GitHub 自动回退限频每 5 分钟一次，防匿名配额耗尽）。

## v1.0.8

- 修复：**在线更新完成后未自动重启**——新实例启动时被单实例互斥锁当成"重复副本"退出
  （旧实例还没退完）。现在更新重启会等待旧实例退出后接管。
- 自动检查更新间隔 3 小时 → **30 分钟**，新版本发布后能及时在底部状态栏弹出提示。

## v1.0.7

- 实时行情表格补齐腾讯行情携带的**全部字段列**：成交量/成交额/总市值默认显示，
  流通市值/换手率/量比/振幅/均价/市盈TTM/市净率/涨停价/跌停价/52周最高/52周最低/股息率/外盘/内盘/时间
  默认隐藏（右键列头勾选，配置自动记忆）。
- 列表中大数**进位显示**（3110123 → 311.01万，市值到亿/万亿），悬停显示真实值；
  下方选中行的字段徽章保持真实值。金额/市值已按市场归一化（A股/港/美/韩单位各不同）。

## v1.0.6

- 在线更新改为**底部状态栏提示条**：定时(每 3 小时)后台检查，有新版在下方弹出提示，
  可选择「更新」或「关闭」；手动「检查更新」发现新版也走同一提示条。关闭后同版本不再自动打扰。

## v1.0.5

- 修复：拖拽指示线在**行间缝隙处**仍会瞬移到最后一行——改为按行中线判断，每个位置都有确定归属。

## v1.0.4

- 修复：拖拽的插入指示线**时不时瞬移到最后一行**——改为按光标坐标命中所在行，
  不再依赖光标下的元素（拖动中它常落在行外，导致跳位）。

## v1.0.3

- 拖拽调整顺序时新增**插入位置指示线**：拖动中在目标行的上/下边缘画一条线，
  按光标在目标行的上半/下半区精确决定插到它**前面**还是**后面**，不再模棱两可。

## v1.0.2

- 实时行情列排序改为**三态循环**：正序 → 反序 → **原序**（用户手动拖拽的顺序）。
- 修复：排序激活时拖拽调整顺序无效——现在"原序"态才允许拖拽，避免拖错行。

## v1.0.1

- 新增**在线版本检查与自动更新**：**国内 NAS 源优先**（`nas.pcguan.cn/quoteview/`）、GitHub 兜底，
  发现新版一键下载、改名替换、自动重启。
- 标题栏新增「检查更新」按钮；启动后台静默检查（仅有新版才提示）。

## v1.0.0

- 首个发布版本。
- 六交易所（沪A / 深A / 北交所 / 港股 / 美股 / 韩股）合约查询、实时行情、K 线 / 分时图。
- 桌面置顶简洁行情面板：多行、透明度、字段/配色、行距、分时缩略图、全局快捷键。
- 分组管理：拖拽排序、导入导出、列布局记忆、板块字段（行业/地区/概念）。
